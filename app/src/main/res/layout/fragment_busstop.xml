<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/fm_busstop"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center_vertical"
    android:gravity="center_vertical"
    android:orientation="vertical">
    <ListView
        android:id="@+id/busstop_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="#dddddd"
        android:scrollbars="vertical"
        android:dividerHeight="4dp">
    </ListView>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="30dp"
        android:layout_marginBottom="55dp"
        android:id="@+id/busstop_refresh_fab"
        android:windowSoftInputMode = "adjustResize"
        android:fitsSystemWindows ="true"
        android:backgroundTint="@color/busblue"
        android:clickable="true"
        android:visibility="visible"
        android:elevation="6dp"
        app:rippleColor="@color/white"
        app:fabSize="normal"
        android:src="@drawable/ic_refresh_black_24dp"/>


    <!--        android:visibility="gone"-->
    <TextView
        android:id="@+id/busstop_empty_tv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"

        android:text="정류소 정보가 없습니다."
        android:textColor="@color/gray"
        android:textStyle="bold"
        android:textSize="14sp"
        android:gravity="center"/>

    <ScrollView android:id="@+id/layout_add_busstop"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="#000000"
        android:padding="8dp"
        android:visibility="gone">
    </ScrollView>

</RelativeLayout>